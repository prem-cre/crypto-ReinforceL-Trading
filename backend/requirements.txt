# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
websockets==13.1
python-multipart==0.0.20

# Config / validation
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
email-validator==2.2.0

# Database (Neon Postgres + pgvector)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
psycopg2-binary==2.9.10  # alembic offline mode + sync utilities

# Auth
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-jose[cryptography]==3.3.0

# Exchange + data
ccxt==4.4.40
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1
ta==0.11.0

# ML / RL (CPU-only torch keeps the Docker image under 1.5GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
safetensors==0.4.5

# Model registry + LLM/RAG (Phase 2 ready)
huggingface_hub==0.26.5
google-generativeai==0.8.3

# MLOps (Phase 4 ready)
wandb==0.19.1

# Observability (Phase 4 ready)
prometheus-fastapi-instrumentator==7.0.0
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
